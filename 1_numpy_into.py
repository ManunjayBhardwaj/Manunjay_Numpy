import numpy as np
#here we are importing numpy as an alias named np this means that when we further need numpy we will use it as np and not numpy
print(np.__version__)
#this function prints the version of numpy
